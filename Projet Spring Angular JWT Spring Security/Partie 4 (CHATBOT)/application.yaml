# OpenAI Configuration
spring:
  ai:
    openai:
      api-key: ${je peux pas le publier}
      chat:
        options:
          model: gpt-3.5-turbo
          temperature: 0.7

# Telegram Bot Configuration
telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:je peux pas le publier}
    username: ${TELEGRAM_BOT_USERNAME:je peux pas le publier}